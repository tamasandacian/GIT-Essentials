
Identify merged branches:

git branch --merged						: shows which branches are merged
git branch --no-merged					: shows which branches are not merged
git branch -r --merged					: shows which branches are merged in remote



git branch
	feature_branch_1
	feature_branch_2
	*master

git checkout feature_branch_1

git branch
	*feature_branch_1
	feature_branch_2
	master

git branch --merged
	master
	feature_branch_1

git branch --no-merged
	feature_branch_2

git checkout master
git checkout feature_branch_2

git branch --merged
	feature_branch_2

git checkout master
git merge feature_branch-2

git branch -r --merged				: check merged branches with remote

					